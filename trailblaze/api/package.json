{
  "name": "trailblaze-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply trailblaze-db",
    "db:migrate:local": "wrangler d1 migrations apply trailblaze-db --local"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240529.0",
    "wrangler": "^3.60.0",
    "typescript": "^5.0.0"
  }
}
